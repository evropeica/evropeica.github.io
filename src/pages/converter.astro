---
import Base from '../layouts/Base.astro';
---

<Base title="Konverter" description="Live transliteration converter — type in Cyrillic, see Latin output">
  <h1>Konverter / Конвертер</h1>

  <p>Vvedíť tekst kyryłyceju — pobačyte rezultat łatynkoju v reálnomu časi.</p>
  <p class="cyr">Введіть текст кирилицею — побачите результат латинкою в реальному часі.</p>

  <div class="converter">
    <div class="converter-panel">
      <label for="input">Кирилиця (input)</label>
      <textarea id="input" rows="8" placeholder="Введіть текст українською..."></textarea>
    </div>
    <div class="converter-panel">
      <label for="output">Латинка (output)</label>
      <textarea id="output" rows="8" readonly></textarea>
    </div>
  </div>

  <div class="examples">
    <h3>Prykłady / Приклади</h3>
    <div class="example-chips">
      <button class="chip" data-text="Київ — столиця України">Київ</button>
      <button class="chip" data-text="Щастя не купиш за гроші">Щастя</button>
      <button class="chip" data-text="Всі люди народжуються вільними і рівними у своїй гідності та правах">Декларація</button>
      <button class="chip" data-text="М'яч, п'ять, об'єм, кур'єр">Апостроф</button>
      <button class="chip" data-text="Джерело, дзвін, підживити, надзвичайний">Диграфи</button>
      <button class="chip" data-text="Ще не вмерла України ні слава, ні воля">Гімн</button>
    </div>
  </div>
</Base>

<script>
  import { transliterate } from '../lib/transliterate';

  const input = document.getElementById('input') as HTMLTextAreaElement;
  const output = document.getElementById('output') as HTMLTextAreaElement;

  input.addEventListener('input', () => {
    output.value = transliterate(input.value);
  });

  document.querySelectorAll('.chip').forEach(btn => {
    btn.addEventListener('click', () => {
      const text = (btn as HTMLElement).dataset.text || '';
      input.value = text;
      output.value = transliterate(text);
    });
  });
</script>

<style>
  .cyr { color: var(--color-muted); font-style: italic; }

  .converter {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    margin: 1.5rem 0;
  }

  @media (max-width: 640px) {
    .converter { grid-template-columns: 1fr; }
  }

  .converter-panel label {
    display: block;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 0.4rem;
    color: var(--color-muted);
  }

  textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    font-family: var(--font-sans);
    font-size: 1rem;
    line-height: 1.6;
    resize: vertical;
    background: white;
  }

  textarea:focus {
    outline: none;
    border-color: var(--color-accent);
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
  }

  textarea[readonly] {
    background: #f9fafb;
    color: var(--color-text);
  }

  .examples { margin-top: 2rem; }

  .example-chips {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-top: 0.75rem;
  }

  .chip {
    padding: 0.4rem 0.8rem;
    border: 1px solid var(--color-border);
    border-radius: 20px;
    background: white;
    cursor: pointer;
    font-size: 0.85rem;
    transition: all 0.15s;
  }

  .chip:hover {
    border-color: var(--color-accent);
    background: #eff6ff;
  }
</style>
